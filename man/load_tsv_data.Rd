% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Front_pareto.R
\name{load_tsv_data}
\alias{load_tsv_data}
\title{Load TSV Data for a Given Optimal Point}
\usage{
load_tsv_data(optimal_point, point_name, tsv_dir)
}
\arguments{
\item{optimal_point}{A list or data frame row containing at least
the fields \code{lambda_} and \code{gamma}.}

\item{point_name}{A string used in warning messages to identify the point.}

\item{tsv_dir}{The directory where TSV files are stored.}
}
\value{
A data frame read from the corresponding TSV file, or \code{NULL} if
no matching file is found.
}
\description{
This function searches for a TSV file corresponding to the provided
optimal point, identified by \code{lambda_} and \code{gamma}. File names are
expected to follow the pattern \code{"H_lambda_<lambda>_gamma_<gamma>.tsv"},
where \verb{<lambda>} and \verb{<gamma>} are truncated to two decimal places
(not rounded). The function reads the first matching TSV file it finds.
}
\examples{
\dontrun{
tsv_dir <- "path/to/tsv/files"
load_tsv_data(optimal_points$DiceproOptCstrt, "DiceproOptCstrt", tsv_dir)
}

}
