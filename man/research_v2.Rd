% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypersearch.R
\name{research_v2}
\alias{research_v2}
\title{Hyperparameter optimization for DICEPRO}
\usage{
research_v2(
  objective_opt,
  dataset,
  config_path,
  hp_space = NULL,
  report_path = NULL,
  W_prime = NULL
)
}
\arguments{
\item{objective_opt}{Function. Objective function to optimize. Must return a list containing a numeric \code{loss}.}

\item{dataset}{List. Dataset containing matrices such as bulk expression, reference signatures, and precomputed proportions.}

\item{config_path}{Character. Path to JSON configuration file specifying experiment settings (exp path, hp_max_evals, hp_method, hp_space, seed).}

\item{hp_space}{List (optional). User-defined hyperparameter search space. If \code{NULL}, parsed from configuration file.}

\item{report_path}{Character (optional). Path to store intermediate results. If \code{NULL}, defaults to \code{config$exp/results}.}

\item{W_prime}{Description of W_prime}
}
\value{
List with:
\describe{
\item{best}{Best hyperparameter set found.}
\item{trials}{List of all trial results, each containing \code{params} and \code{result}.}
}
}
\description{
Performs hyperparameter optimization using a specified objective function.
Supports user-defined search spaces or configuration-defined search spaces.
}
\details{
\enumerate{
\item Load configuration from JSON.
\item Create or parse hyperparameter search space.
\item Loop for \code{hp_max_evals} iterations:
\itemize{
\item Sample a parameter set from the search space.
\item Evaluate objective function with \code{objective_wrapper()} (handles errors and saves JSON).
\item Track the best parameters based on the \code{loss}.
}
\item Return the best parameters and full trial history.
}
}
\examples{
\dontrun{
config_path <- "path/to/optim_config.json"
dataset <- list(B = bulk_matrix, W = reference_matrix, P = precomputed_proportions)
result <- research_v2(my_objective_function, dataset, config_path)
}

}
